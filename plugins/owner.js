const {command} = require ('../lib')

command(
  {
     pattern: 'owner',
     fromMe: true,
     desc: 'Amarok owner',
  },
async (message,match) => {
const buttons = [
  {index: 1, urlButton: {displayText: 'GITHUB!', URL:'https://GitHub.com/Diegoson'}},
]
}
)
